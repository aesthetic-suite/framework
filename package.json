{
  "name": "aesthetic-root",
  "private": true,
  "scripts": {
    "build": "beemo typescript --workspaces=aesthetic --priority --react",
    "coverage": "yarn run jest --coverage",
    "eslint": "beemo eslint",
    "jest": "beemo jest --react",
    "prettier": "beemo prettier",
    "release": "lerna publish",
    "type": "beemo typescript --workspaces=* --noEmit --react",
    "prerelease": "yarn test && yarn run build",
    "pretest": "yarn run type",
    "test": "yarn run jest",
    "posttest": "yarn run eslint"
  },
  "devDependencies": {
    "@milesj/build-tool-config": "^0.113.0",
    "babel-loader": "^7.1.5",
    "babel-plugin-css-modules-transform": "1.6.1",
    "babel-register": "^6.26.0",
    "chalk": "^2.4.1",
    "fs-promise": "^2.0.3",
    "glob": "^7.1.2",
    "lerna": "^2.11.0"
  },
  "beemo": {
    "module": "@milesj/build-tool-config",
    "drivers": [
      "babel",
      "eslint",
      "jest",
      "prettier",
      "typescript"
    ],
    "babel": {
      "plugins": [
        "css-modules-transform"
      ]
    }
  },
  "workspaces": [
    "packages/*"
  ],
  "browserslist": [
    "ie 10"
  ]
}
