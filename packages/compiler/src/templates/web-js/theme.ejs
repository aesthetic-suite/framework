// Auto-generated by Aesthetic!

import designSystem from '../system';
<% if (theme.extendedFrom) { %>
import parentTheme from './<%= theme.extendedFrom %>';
<% } %>

<% if (theme.extendedFrom) { %>
const <%= fileName %>Theme = parentTheme.extend(<%= JSON.stringify(template) %>, {
  contrast: '<%= theme.contrast %>',
  scheme: '<%= theme.scheme %>',
});
<% } else { %>
const <%= fileName %>Theme = designSystem.createTheme({
  contrast: '<%= theme.contrast %>',
  scheme: '<%= theme.scheme %>',
}, <%= JSON.stringify(template) %>);
<% } %>

export default <%= fileName %>Theme;
