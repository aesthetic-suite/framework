// Auto-generated by Aesthetic!

import { Design } from '@aesthetic/core';

export default new Design({
  border: {
    <% borderSizes.forEach(size => { %>
    <%= size %>: {
      radius: '<%= platform.rem(template.border[size].radius) %>',
      width: '<%= platform.rem(template.border[size].width) %>',
    },
    <% } %>
  },
  breakpoint: {
    <% breakpointSizes.forEach(size => { %>
    <%= size %>: {
      query: '<%= platform.query(template.breakpoint[size].queryConditions) %>',
      querySize: <%= template.breakpoint[size].querySize %>,
      rootLineHeight: <%= template.breakpoint[size].rootLineHeight %>,
      rootTextSize: '<%= platform.rem(template.breakpoint[size].rootTextSize) %>',
    },
    <% } %>
  },
  heading: {
    <% headingSizes.forEach(size => { %>
    <%= size %>: {
      letterSpacing: '<%= platform.px(template.heading[size].letterSpacing) %>',
      lineHeight: <%= template.heading[size].lineHeight %>,
      size: '<%= platform.rem(template.heading[size].size) %>',
    },
    <% } %>
  },
  spacing: {
    <% spacingSizes.forEach(size => { %>
    <%= size %>: '<%= template.spacing[size] %>rem',
    <% } %>
  },
  text: {
    <% textSizes.forEach(size => { %>
    <%= size %>: {
      lineHeight: <%= template.text[size].lineHeight %>,
      size: '<%= platform.rem(template.text[size].size) %>',
    },
    <% } %>
  },
  typography: {
    font: <%= JSON.stringify(template.typography.font) %>,
    rootLineHeight: <%= template.text.df.lineHeight %>,
    rootTextSize: '<%= platform.rem(template.text.df.size) %>',
  };
});
