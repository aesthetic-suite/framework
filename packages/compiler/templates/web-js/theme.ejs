// Auto-generated by Aesthetic. Do not modify!
<% if (theme.extendedFrom) { -%>
import parentTheme from './<%= theme.extendedFrom -%>';

export default parentTheme.extend({
  palette: <%- JSON.stringify(data.palette) -%>,
}, {
  contrast: '<%= theme.contrast -%>',
  scheme: '<%= theme.scheme -%>',
});

<% } else { -%>
import design from '../<%= design.name -%>';

export default design.createTheme({
  contrast: '<%= theme.contrast -%>',
  scheme: '<%= theme.scheme -%>',
}, {
  palette: <%- JSON.stringify(data.palette) -%>,
});
<% } -%>
