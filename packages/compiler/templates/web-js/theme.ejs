// contrast: <%= theme.contrast %>
// scheme: <%= theme.scheme %>

export * from '..';

<% Object.entries(data.palette).forEach(([name, palette]) => { -%>
export const <%= name -%> = <%- include('../partials/web/palette', { palette }) -%>;

<% }) -%>
