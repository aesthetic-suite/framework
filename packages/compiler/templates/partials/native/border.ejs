{
  <% borderSizes.forEach(size => { -%>
  <%= size -%>: {
    radius: <%= platform.unit(data.border[size].radius) -%>, // <%= platform.px(data.border[size].radius) %>
    width: <%= platform.unit(data.border[size].width) -%>, // <%= platform.px(data.border[size].width) %>
  },
  <% }) -%>
}
