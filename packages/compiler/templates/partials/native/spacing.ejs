{
  <% spacingSizes.forEach(size => { -%>
  <%= size -%>: <%= platform.unit(platform.spacing(data.spacing[size])) -%>, // <%= platform.px(platform.spacing(data.spacing[size])) %>
  <% }) -%>
  type: '<%= data.spacing.type -%>',
  unit: <%= data.spacing.unit -%>,
}
