import { border } from './border';
import { box } from './box';
import { heading } from './heading';
import {
  hidden,
  hiddenOffscreen,
  resetButton,
  resetInput,
  resetList,
  resetTypography,
  textBreak,
  textTruncate,
  textWrap,
} from './pattern';
import { shadow } from './shadow';
import { text } from './text';

export default {
  border,
  box,
  heading,
  hidden,
  hiddenOffscreen,
  resetButton,
  resetInput,
  resetList,
  resetTypography,
  shadow,
  text,
  textBreak,
  textTruncate,
  textWrap,
};
