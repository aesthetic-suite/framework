import { border } from './border';
import { box } from './box';
import { heading } from './heading';
import {
  hideCompletely,
  hideOffscreen,
  hideVisually,
  resetButton,
  resetInput,
  resetList,
  resetMedia,
  resetTypography,
  root,
  textBreak,
  textTruncate,
  textWrap,
} from './pattern';
import { shadow } from './shadow';
import { text } from './text';

export default {
  border,
  box,
  heading,
  hideCompletely,
  hideOffscreen,
  hideVisually,
  resetButton,
  resetInput,
  resetList,
  resetMedia,
  resetTypography,
  root,
  shadow,
  text,
  textBreak,
  textTruncate,
  textWrap,
};
